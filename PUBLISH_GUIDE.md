# 📚 Полное руководство по публикации ChatList

## 📋 Содержание
1. [Подготовка к релизу](#подготовка-к-релизу)
2. [Создание релиза вручную](#создание-релиза-вручную)
3. [Автоматическая публикация через GitHub Actions](#автоматическая-публикация)
4. [Настройка GitHub Pages](#настройка-github-pages)
5. [Обновление лендинга](#обновление-лендинга)

---

## 🚀 Подготовка к релизу

### Шаг 1: Обновление версии

1. Откройте `version.py`
2. Измените версию:
   ```python
   __version__ = "1.0.1"  # Новая версия
   ```
3. Сохраните файл

### Шаг 2: Коммит изменений

```powershell
git add version.py
git commit -m "Bump version to 1.0.1"
git push origin main
```

### Шаг 3: Сборка приложения

```powershell
.\build_installer.ps1
```

Или вручную:
```powershell
# Получить версию
$version = python -c "import version; print(version.__version__)"

# Собрать исполняемый файл
pyinstaller --onefile --windowed --name "ChatList_v$version" --icon=app.ico main.py

# Создать инсталлятор (если установлен Inno Setup)
"C:\Program Files (x86)\Inno Setup 6\ISCC.exe" setup.iss
```

### Шаг 4: Тестирование

- [ ] Запустить исполняемый файл
- [ ] Проверить основные функции
- [ ] Протестировать инсталлятор
- [ ] Убедиться, что все работает корректно

---

## 📦 Создание релиза вручную

### Вариант 1: Через веб-интерфейс GitHub

1. **Перейдите на страницу репозитория**
   - Откройте: `https://github.com/ваш-username/ChatList`
   - Нажмите "Releases" в правой панели

2. **Создайте новый релиз**
   - Нажмите "Create a new release" или "Draft a new release"

3. **Заполните информацию**
   - **Tag version**: `v1.0.1` (соответствует версии в `version.py`)
   - **Target**: `main` (или нужная ветка)
   - **Release title**: `ChatList v1.0.1`
   - **Description**: 
     - Скопируйте содержимое из `RELEASE_NOTES_TEMPLATE.md`
     - Обновите версию и описание изменений

4. **Загрузите файлы**
   - Перетащите `dist\ChatList_v1.0.1.exe` в область загрузки
   - Перетащите `installer\ChatList_Setup_v1.0.1.exe` в область загрузки
   - Или используйте кнопку "Attach binaries"

5. **Опубликуйте**
   - Нажмите "Publish release"

### Вариант 2: Через GitHub CLI

```powershell
# Установите GitHub CLI если еще не установлен
# https://cli.github.com/

# Авторизуйтесь
gh auth login

# Создайте тег
git tag v1.0.1
git push origin v1.0.1

# Создайте релиз
gh release create v1.0.1 `
  --title "ChatList v1.0.1" `
  --notes-file RELEASE_NOTES_TEMPLATE.md `
  "dist\ChatList_v1.0.1.exe#ChatList v1.0.1 (Portable)" `
  "installer\ChatList_Setup_v1.0.1.exe#ChatList v1.0.1 (Installer)"
```

---

## 🤖 Автоматическая публикация

### Настройка GitHub Actions

1. **Файл уже создан**: `.github/workflows/release.yml`

2. **Как это работает**:
   - При создании тега (например, `v1.0.1`) автоматически запускается сборка
   - Создается релиз с файлами
   - Все происходит автоматически!

3. **Использование**:
   ```powershell
   # Создайте тег
   git tag v1.0.1
   git push origin v1.0.1
   ```

4. **Проверка**:
   - Перейдите в "Actions" на GitHub
   - Увидите запущенный workflow
   - После завершения релиз будет создан автоматически

---

## 🌐 Настройка GitHub Pages

### Шаг 1: Включение Pages

1. Перейдите в **Settings** → **Pages**
2. В разделе **Source**:
   - **Branch**: выберите `gh-pages` или `main`
   - **Folder**: выберите `/docs`
3. Нажмите **Save**

### Шаг 2: Подготовка файлов

Файл `docs/index.html` уже создан. Обновите его:

1. Откройте `docs/index.html`
2. Замените `ваш-username` на ваш GitHub username
3. Обновите ссылки на релизы (если нужно)

### Шаг 3: Публикация

```powershell
git add docs/index.html
git commit -m "Update GitHub Pages landing page"
git push origin main
```

GitHub Pages автоматически обновится через несколько минут.

### Автоматическое обновление Pages

Workflow `.github/workflows/pages.yml` автоматически обновляет Pages при изменении файлов в `docs/`.

---

## 📝 Обновление лендинга

### Обновление версии в HTML

1. Откройте `docs/index.html`
2. Найдите все упоминания версии:
   ```html
   <div class="version-badge">Версия 1.0.0</div>
   ```
3. Обновите на новую версию
4. Обновите ссылки на релизы:
   ```html
   href="https://github.com/ваш-username/ChatList/releases/latest/download/ChatList_Setup_v1.0.1.exe"
   ```

### Обновление описания

Отредактируйте секции в `docs/index.html`:
- Features (возможности)
- Requirements (требования)
- Quick Start (быстрый старт)

---

## ✅ Чеклист перед релизом

- [ ] Версия обновлена в `version.py`
- [ ] Все изменения закоммичены и запушены
- [ ] Исполняемый файл собран и протестирован
- [ ] Инсталлятор создан и протестирован
- [ ] `RELEASE_NOTES_TEMPLATE.md` обновлен
- [ ] `docs/index.html` обновлен (версия и ссылки)
- [ ] Тег создан: `git tag v1.0.1`
- [ ] Тег отправлен: `git push origin v1.0.1`
- [ ] GitHub Release создан (вручную или автоматически)
- [ ] Файлы загружены в Release
- [ ] GitHub Pages обновлен

---

## 🔗 Полезные ссылки

- [GitHub Releases](https://github.com/ваш-username/ChatList/releases)
- [GitHub Pages](https://ваш-username.github.io/ChatList/)
- [GitHub Actions](https://github.com/ваш-username/ChatList/actions)
- [Issues](https://github.com/ваш-username/ChatList/issues)

---

## 💡 Советы

1. **Семантическое версионирование**: Используйте формат `MAJOR.MINOR.PATCH`
   - MAJOR: несовместимые изменения API
   - MINOR: новая функциональность (обратно совместимая)
   - PATCH: исправления ошибок

2. **Тестирование**: Всегда тестируйте сборку перед релизом

3. **Заметки о релизе**: Подробно описывайте изменения для пользователей

4. **Автоматизация**: Используйте GitHub Actions для экономии времени

---

## 🆘 Решение проблем

### Проблема: GitHub Actions не запускается
- Проверьте, что файл `.github/workflows/release.yml` существует
- Убедитесь, что тег создан правильно: `v1.0.1`
- Проверьте права доступа в Settings → Actions

### Проблема: GitHub Pages не обновляется
- Проверьте настройки в Settings → Pages
- Убедитесь, что файл `docs/index.html` существует
- Подождите несколько минут (обновление не мгновенное)

### Проблема: Файлы не загружаются в Release
- Проверьте размер файлов (максимум 2GB на файл)
- Убедитесь, что файлы существуют в указанных путях
- Проверьте права доступа

---

**Удачи с публикацией! 🚀**

