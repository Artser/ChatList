# ChatList

Приложение для сравнения ответов разных нейросетей на один промт. Позволяет отправлять один и тот же запрос в несколько моделей одновременно и сравнивать их ответы.

## Возможности

- 📝 Отправка промтов в несколько нейросетей одновременно
- 💾 Сохранение промтов и результатов в базе данных SQLite
- 🔍 Поиск и сортировка по всем таблицам
- 📊 Временная таблица результатов с возможностью выбора для сохранения
- 🎯 Поддержка различных API: OpenAI, DeepSeek, Groq, OpenRouter
- 📤 Экспорт результатов в Markdown и JSON
- ⚙️ Управление моделями через удобный интерфейс
- 📋 Контекстные меню для быстрого доступа к функциям
- 📝 Логирование всех запросов в файлы

## Установка

### Требования

- Python 3.11+
- Windows (для сборки .exe)

### Шаги установки

1. Клонируйте репозиторий:
```powershell
git clone https://github.com/Artser/ChatList.git
cd ChatList
```

2. Установите зависимости:
```powershell
pip install -r requirements.txt
```

3. Настройте API-ключи:
   - Скопируйте `.env.example` в `.env`
   - Заполните API-ключи для нужных сервисов:
   ```
   OPENAI_API_KEY=sk-your-key-here
   DEEPSEEK_API_KEY=sk-your-key-here
   GROQ_API_KEY=gsk_your-key-here
   OPENROUTER_API_KEY=sk-or-your-key-here
   ```

## Запуск

Запустите приложение:

```powershell
python main.py
```

## Использование

### Добавление моделей

1. Перейдите на вкладку "Модели"
2. Нажмите "Добавить модель"
3. Заполните форму:
   - **Название**: Имя модели (например, "GPT-4")
   - **API URL**: URL API (например, `https://api.openai.com/v1/chat/completions`)
   - **API ID**: Имя переменной окружения с ключом (например, `OPENAI_API_KEY`)
   - **Активна**: Чекбокс для активации/деактивации модели

### Отправка запросов

1. На вкладке "Запросы" введите промт
2. (Опционально) Добавьте теги через запятую
3. Нажмите "Отправить запрос"
4. Дождитесь ответов от всех активных моделей
5. Выберите результаты для сохранения и нажмите "Сохранить выбранные"

### Работа с результатами

- Просмотр сохраненных результатов на вкладке "Результаты"
- Поиск по тексту ответа, промту или модели
- Экспорт выбранных результатов в Markdown или JSON
- Удаление результатов через контекстное меню

### Поиск промтов

- Используйте кнопку "Поиск" рядом с выпадающим списком промтов
- Или введите текст в поле выпадающего списка для фильтрации

## Сборка исполняемого файла

Для создания исполняемого .exe файла используйте скрипт сборки:

```powershell
.\build.ps1
```

Или вручную с помощью PyInstaller:

```powershell
pyinstaller --onefile --windowed --name "ChatList" main.py
```

Исполняемый файл будет находиться в папке `dist\ChatList.exe`.

### Параметры сборки:
- `--onefile` - создает один исполняемый файл
- `--windowed` - скрывает консольное окно (для GUI приложений)
- `--name "ChatList"` - имя выходного файла

## Структура проекта

```
ChatList/
├── main.py              # Основной модуль с GUI
├── db.py                # Работа с базой данных
├── models.py            # Логика работы с моделями
├── network.py           # Отправка запросов к API
├── requirements.txt     # Зависимости
├── PROJECT.md           # Спецификация проекта
├── PLAN.md              # План реализации
├── DATABASE.md          # Схема базы данных
├── .env.example         # Пример файла с API-ключами
└── build.ps1            # Скрипт сборки
```

## Поддерживаемые API

- **OpenAI** - GPT-3.5, GPT-4 и другие модели OpenAI
- **DeepSeek** - DeepSeek Chat
- **Groq** - Быстрые модели через Groq API
- **OpenRouter** - Доступ к множеству моделей через единый API

## Логирование

Все запросы к API логируются в файлы в папке `logs/`. Файлы создаются ежедневно с именем `chatlist_YYYYMMDD.log`.

## База данных

Приложение использует SQLite для хранения данных. База данных создается автоматически при первом запуске. Структура БД описана в файле `DATABASE.md`.

## Лицензия

См. файл `LICENSE`.

## Разработка

Для разработки и тестирования:

1. Убедитесь, что все зависимости установлены
2. Настройте `.env` файл с тестовыми API-ключами
3. Запустите приложение: `python main.py`
4. Проверьте логи в папке `logs/`

## Известные ограничения

- Таймаут запросов по умолчанию: 30 секунд (настраивается в меню "Настройки")
- Максимальное количество параллельных запросов: 5
- База данных SQLite не подходит для больших объемов данных (>1GB)

## Поддержка

При возникновении проблем:
1. Проверьте логи в папке `logs/`
2. Убедитесь, что API-ключи правильно настроены в `.env`
3. Проверьте, что модели активны в интерфейсе
